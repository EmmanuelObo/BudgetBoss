import { Injectable } from "@angular/core";
import { Item, List } from '../_models/index';
import { Priority } from '../_constants/index';

@Injectable()
export class SortingService{
    by_priority(items: Item[])
    {
    }
}